language: java

jdk:
    - oraclejdk8

git:
    depth: 3

before_script:
    - chmod +x gradlew

script:
    - echo 'Setup environment'
    - ./gradlew setupDecompWorkspace eclipse
    - echo 'Build'
    - ./gradlew build